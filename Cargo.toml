[package]
name = "ww-bear"
version = "0.1.0"
edition = "2021"

[dependencies]
serial2 = { version = "0.2.28", optional = true }
log = "0.4.25"
derive_more = { version = "2.0.1", features = ["from", "error", "display", "debug"] }

[dev-dependencies]
test-log = "0.2.17"
env_logger = "0.11.6"

[features]
default = ["std", "serial2"]
alloc = []
std = ["alloc"]
